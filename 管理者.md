# 管理者功能說明文件

## 概覽

本系統提供管理者專用頁面，用於查詢捐血民眾資料、更新禮品領取狀態，以及控制系統開關。

---

## 頁面清單

| 頁面 | 網址 | 說明 |
|------|------|------|
| 管理者登入 | `oavwfc2008-admin-login.html` | 輸入密碼驗證身分 |
| 禮品領取管理 | `admin-gift.html` | 查詢資料 / 更新禮品領取狀態 |

> 兩個頁面**不放在首頁入口**，需直接用網址進入。

---

## 登入機制

### 密碼儲存方式

- 密碼以 **SHA-256 hash** 儲存
- Hash 值不會出現在任何前端程式碼或 GitHub 儲存庫中

### Token 驗證機制

1. 管理者輸入密碼 → 前端送出至 GAS
2. GAS 將輸入密碼 SHA-256 後與儲存的 hash 比對
3. 比對成功 → GAS 產生一個**小時級 token**（`SHA-256(hash:當前小時數)`）回傳
4. Token 存於瀏覽器 `sessionStorage`（關閉分頁即自動清除）
5. 後續每次 API 呼叫都帶上 token，GAS 驗證後才執行操作
6. Token 有效期：**1 ~ 2 小時**（跨整點時有一小時緩衝）

### Session 失效情境

| 情境 | 結果 |
|------|------|
| 關閉瀏覽器分頁 | sessionStorage 清除，需重新登入 |
| Token 逾時（超過 2 小時） | API 回傳 `unauthorized`，自動跳轉登入頁 |
| 手動點擊「登出」 | 清除 sessionStorage，跳轉登入頁 |

---

## 初次設定密碼

在 Google Apps Script 編輯器中，貼上以下函式並執行一次，執行後刪除：

```javascript
function setAdminPassword() {
  const plain = '這裡填你的密碼';
  const digest = Utilities.computeDigest(Utilities.DigestAlgorithm.SHA_256, plain);
  const hash = digest.map(function(b) { return ('0' + (b & 0xff).toString(16)).slice(-2); }).join('');
  PropertiesService.getScriptProperties().setProperty('ADMIN_PASSWORD', hash);
  Logger.log('已儲存 hash：' + hash);
}
```

執行步驟：
1. 開啟 Google Sheet → 擴充功能 → Apps Script
2. 在編輯器底部貼上上方函式
3. 函式選單切換為 `setAdminPassword` → 點擊 ▶ 執行
4. 確認執行記錄顯示 hash 值後，刪除此函式
5. 儲存並**重新部署** GAS（管理部署作業 → 新版本 → 部署）

> 之後若要變更密碼，重複以上步驟即可，**不需修改前端程式碼**。

---

## Script Properties 說明

GAS 使用 Script Properties 儲存系統設定（不存在程式碼或前端）：

| 屬性名稱 | 說明 | 值 |
|----------|------|----|
| `ADMIN_PASSWORD` | 管理者密碼的 SHA-256 hash | 64 位十六進位字串 |
| `SYSTEM_ACTIVE` | 系統開關狀態 | `true`（開啟）/ `false`（關閉） |
| `CHECKIN_ACTIVE` | 報到功能開關狀態 | `true`（開放報到）/ `false` 或未設定（關閉報到） |
| `CURRENT_CALL_NUMBER` | 首頁顯示的當前叫號 | 1～4 碼數字，系統顯示為 4 碼 |
| `LAST_CHECKIN_NUMBER` | 最後成功寫入的報到編號紀錄，供管理者檢查與欄位整理同步 | 數字字串 |

> `SYSTEM_ACTIVE` 未設定時預設為**開啟**（程式碼判斷 `!== 'false'`）。
> 報到發號以 Sheet 目前最大報到編號為準，`LAST_CHECKIN_NUMBER` 不直接決定下一號；若管理者手動把報到編號改成更大的號碼，系統也會同步更新此紀錄值。

### 報到編號欄位整理

若 Google Sheet 既有資料出現 `90`、`91` 這類未補零顯示，可在 Apps Script 編輯器手動執行一次 `normalizeCheckinNumberColumn()`。

此函式會：
- 將 G 欄既有報到編號轉成可套用格式的數字
- 將 G 欄顯示格式設為 `0000`
- 依 Sheet 目前最大報到編號同步更新 `LAST_CHECKIN_NUMBER`

### 同時報到等待時間

報到編號產生流程使用 `LockService` 排隊處理，避免多人同時送出造成重複號碼。已報名報到與未報名現場報到的等待時間由 `CHECKIN_LOCK_WAIT_MS` 控制，目前設定為 `30000` 毫秒。

若 30 秒內仍無法取得鎖，使用者會看到「系統繁忙，請稍後再試。」一般報名與完成報名仍維持原本 15 秒等待時間。

---

## GAS 新增函式說明

| 函式 | Action 名稱 | 說明 |
|------|------------|------|
| `adminLogin` | `admin_login` | 驗證密碼，回傳 token |
| `validateAdminToken` | — | 內部驗證 token（不對外暴露） |
| `adminLookup` | `admin_lookup` | 依手機電話或報到編號查詢資料 |
| `updateGift` | `update_gift` | 更新指定報到編號的禮品領取欄位（單欄位更新） |
| `adminUpdate` | `admin_update` | 以電話定位，全欄位更新（A～J 欄，電話不可修改） |
| `getSystemStatus` | `get_system_status` | 讀取目前系統開關狀態（需 token） |
| `setSystemStatus` | `set_system_status` | 設定系統開關狀態（需 token） |

---

## admin-gift.html 操作流程

```
1. 開啟頁面 → 自動檢查 sessionStorage token
   └─ 無 token → 跳轉 oavwfc2008-admin-login.html

1-1. 頁面頂部顯示「系統狀態」開關（左右切換）
   ├─ 目前開啟（綠底）：民眾可以使用系統
   └─ 目前關閉（紅底）：民眾無法使用系統，所有 API 請求皆回傳關閉提示
   ※ 點擊後畫面立即切換，同步更新 GAS Script Properties（SYSTEM_ACTIVE）

2. 選擇查詢方式（Tab 切換）
   ├─ 手機電話：輸入 09xxxxxxxx
   └─ 報到編號：輸入 4 位數字

3. 查詢結果預填至編輯表單（10 個欄位）
   ┌─────────┬──────────────────────────┐
   │ 報名時間 │ yyyy/mm/dd               │
   │ 公司行號 │ 下拉選單（含「其他」自填）  │
   │ 姓名     │ 文字輸入                  │
   │ 電話     │ 唯讀，不可修改             │
   │ 捐血CC數 │ 下拉選單（250cc / 500cc） │
   │ 報到時間 │ yyyy/mm/dd HH:mm:ss      │
   │ 報到編號 │ 4 位數字                  │
   │ 報到     │ —（未報到）/ Y（已報到）  │
   │ 捐血成功 │ —（未填）/ Y / N         │
   │ 禮品領取 │ —（未填）/ Y / N / 無符合資格 │
   └─────────┴──────────────────────────┘

4. 修改任意欄位後按下「確認更新」
   → 呼叫 admin_update，更新 Google Sheet A～J 欄（電話除外）

5. 按下「返回查詢」→ 清空表單，可繼續查詢下一位
```

---

## Google Sheet 欄位對應

| 欄 | 欄位名稱 | 說明 |
|----|---------|------|
| A | 報名時間 | 報名日期 |
| B | 公司行號 | 來源單位 |
| C | 姓名 | |
| D | 電話 | 手機號碼（文字格式） |
| E | 捐血CC數 | 250cc / 500cc |
| F | 報到時間 | 報到日期與時間 |
| G | 報到編號 | 4 位流水號（Google Sheet 以 `0000` 格式顯示） |
| H | 報到 | Y / 空白 |
| I | 捐血成功 | Y / N |
| **J** | **禮品領取** | **Y / N / 無符合資格（捐血失敗時系統自動填入；管理者可手動修改）** |
